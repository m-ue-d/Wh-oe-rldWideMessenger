package spg.client;

import java.math.BigDecimal;
import java.math.BigInteger;
import java.util.Scanner;

class RSA
{



    public static BigInteger[] genKeyPair_plusN(){  //first ist private, then is public, then is n
        int e,d,n;

        //hex hex

        return null;
    }

    public static String encode(String msg, BigInteger key){
        return ""; //hex hex
    }
    public static String decode(String msg, BigInteger key){
        return ""; //hex hex
    }
}
